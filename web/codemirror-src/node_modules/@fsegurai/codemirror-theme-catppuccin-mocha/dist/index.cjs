'use strict';

var view = require('@codemirror/view');
var language = require('@codemirror/language');
var highlight = require('@lezer/highlight');

// Helper module for styling options
const generalContent = {
    fontSize: '14px',
    fontFamily: 'JetBrains Mono, Consolas, monospace',
    lineHeight: '1.6',
};
const generalCursor = {
    borderLeftWidth: '2px',
};
const generalDiff = {
    insertedTextDecoration: 'none',
    deletedTextDecoration: 'line-through',
    insertedLinePadding: '1px 3px',
    borderRadius: '3px'};
const generalGutter = {
    border: 'none',
    paddingRight: '8px',
    fontSize: '0.9em',
    fontWeight: '500',
    lineHeight: '1.78', // Adjusted to compensate for 0.9em fontSize (1.6 / 0.9 ≈ 1.78)
};
const generalPanel = {
    border: 'none',
    borderRadius: '4px',
    padding: '2px 10px',
};
const generalLine = {
    borderRadius: '2px',
};
const generalMatching = {
    borderRadius: '2px',
};
const generalPlaceholder = {
    borderRadius: '4px',
    padding: '0 5px',
    margin: '0 2px',
};
const generalScroller = {
    width: '12px',
    height: '12px',
    borderRadius: '6px',
};
const generalSearchField = {
    borderRadius: '4px',
    padding: '2px 6px',
};
const generalTooltip = {
    borderRadius: '4px',
    borderRadiusSelected: '3px',
    lineHeight: '1.3',
    padding: '4px 8px',
    paddingRight: '8px',
};
/**
 * Function to apply merge revert styles for a theme
 * @param styles Styles for the merge revert buttons
 * @param styles.backgroundColor Background color of the revert area
 * @param styles.borderColor Border color of the revert area
 * @param styles.buttonColor Color of the revert buttons
 * @param styles.buttonHoverColor Hover color of the revert buttons
 */
function applyMergeRevertStyles(styles) {
    // Create a stylesheet
    const styleEl = document.createElement('style');
    styleEl.id = 'cm-merge-revert-styles';
    // Define CSS with the theme-specific values
    styleEl.textContent = `
    .cm-merge-revert {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      padding: 4px;
      background-color: ${styles.backgroundColor};
      border-left: 1px solid ${styles.borderColor};
      border-right: 1px solid ${styles.borderColor};
      width: 32px;
    }
    
    .cm-merge-revert button {
      width: 100%;
      height: auto;
      background-color: transparent;
      border: none;
      color: ${styles.buttonColor};
      cursor: pointer;
      margin: 0 auto;
      font-size: 20px;
    }
    
    .cm-merge-revert button:hover {
      background-color: ${styles.buttonHoverColor};
    }
  `;
    // Remove any existing merge styles
    const existingStyle = document.getElementById('cm-merge-revert-styles');
    if (existingStyle)
        existingStyle.remove();
    // Add the new styles
    document.head.appendChild(styleEl);
}

/**
 * Catppuccin Mocha theme color definitions
 * ----------------------------------------
 * A soothing pastel theme with warm tones
 */
// Base colors (Mocha variant)
const base00 = '#1e1e2e', // Background - dark navy
base01 = '#313244', // Selection background
base02 = '#45475a', // Comments
base03 = '#585b70', // Dark grey
base04 = '#cdd6f4', // Foreground - light blue
base05 = '#f5e0dc', // Light foreground
// Catppuccin Mocha colors
rosewater = '#f5e0dc', flamingo = '#f2cdcd', pink = '#f5c2e7', mauve = '#cba6f7', red = '#f38ba8', maroon = '#eba0ac', peach = '#fab387', yellow = '#f9e2af', green = '#a6e3a1', teal = '#94e2d5', sky = '#89dceb', sapphire = '#74c7ec', blue = '#89b4fa', lavender = '#b4befe';
// UI specific colors
const invalid = '#f38ba8', darkBackground = '#181825', background = base00, tooltipBackground = base01, selectionMatch = '#45475a80', cursor = rosewater, activeBracketBg = '#45475a55', activeBracketBorder = lavender, diagnosticWarning = yellow, linkColor = blue, visitedLinkColor = mauve;
// Diff/merge specific colors
const addedBackground = '#a6e3a140', removedBackground = '#f38ba840', addedText = green, removedText = red;
/**
 * Enhanced editor theme styles for Catppuccin Mocha
 */
const catppuccinMochaTheme = view.EditorView.theme({
    // Base editor styles
    '&': {
        color: base04,
        backgroundColor: background,
        fontSize: generalContent.fontSize,
        fontFamily: generalContent.fontFamily,
    },
    // Content and cursor
    '.cm-content': {
        caretColor: cursor,
        lineHeight: generalContent.lineHeight,
    },
    '.cm-cursor, .cm-dropCursor': {
        borderLeftColor: cursor,
        borderLeftWidth: generalCursor.borderLeftWidth,
    },
    '.cm-fat-cursor': {
        backgroundColor: `${cursor}99`,
        color: background,
    },
    // Selection
    '&.cm-focused > .cm-scroller > .cm-selectionLayer .cm-selectionBackground, .cm-selectionBackground, .cm-content ::selection': {
        backgroundColor: '#585b7080',
        outline: `1px solid ${mauve}`,
    },
    // Make sure selection appears above active line
    '.cm-selectionLayer': {
        zIndex: 100,
    },
    // Search functionality
    '.cm-searchMatch': {
        backgroundColor: '#89b4fa40',
        outline: `1px solid ${sapphire}`,
        color: base04,
        borderRadius: generalSearchField.borderRadius,
        '& span': {
            color: base04,
        },
    },
    '.cm-searchMatch.cm-searchMatch-selected': {
        backgroundColor: blue,
        color: base00,
        padding: generalSearchField.padding,
        '& span': {
            color: base00,
        },
    },
    '.cm-search.cm-panel.cm-textfield': {
        color: base04,
        borderRadius: generalSearchField.borderRadius,
        padding: generalSearchField.padding,
    },
    // Panels
    '.cm-panels': {
        backgroundColor: darkBackground,
        color: base04,
        borderRadius: '4px',
    },
    '.cm-panels.cm-panels-top': {
        borderBottom: `1px solid ${base02}`,
    },
    '.cm-panels.cm-panels-bottom': {
        borderTop: `1px solid ${base02}`,
    },
    '.cm-panel button': {
        backgroundColor: tooltipBackground,
        color: base04,
        border: generalPanel.border,
        borderRadius: generalPanel.borderRadius,
        padding: generalPanel.padding,
    },
    '.cm-panel button:hover': {
        backgroundColor: base01,
    },
    // Line highlighting
    '.cm-activeLine': {
        backgroundColor: '#3a3a5266',
        borderRadius: generalLine.borderRadius,
        borderLeft: `2px solid ${lavender}`,
        zIndex: 1,
    },
    // Gutters
    '.cm-gutters': {
        backgroundColor: darkBackground,
        color: base02,
        border: generalGutter.border,
        borderRight: `1px solid ${base01}`,
        paddingRight: generalGutter.paddingRight,
    },
    '.cm-activeLineGutter': {
        backgroundColor: base01,
        color: base04,
        fontWeight: generalGutter.fontWeight,
    },
    '.cm-lineNumbers': {
        fontSize: generalGutter.fontSize,
        lineHeight: generalGutter.lineHeight,
    },
    '.cm-foldGutter': {
        fontSize: generalGutter.fontSize,
        lineHeight: generalGutter.lineHeight,
    },
    '.cm-foldGutter .cm-gutterElement': {
        color: base02,
        cursor: 'pointer',
    },
    '.cm-foldGutter .cm-gutterElement:hover': {
        color: base04,
    },
    // Diff/Merge View Styles
    '.cm-insertedLine': {
        textDecoration: generalDiff.insertedTextDecoration,
        backgroundColor: addedBackground,
        color: addedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadius,
    },
    'ins.cm-insertedLine, ins.cm-insertedLine:not(:has(.cm-changedText))': {
        textDecoration: generalDiff.insertedTextDecoration,
        backgroundColor: `${addedBackground} !important`,
        color: addedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadius,
        border: `1px solid ${addedText}40`,
    },
    'ins.cm-insertedLine .cm-changedText': {
        background: 'transparent !important',
    },
    '.cm-deletedLine': {
        textDecoration: generalDiff.deletedTextDecoration,
        backgroundColor: removedBackground,
        color: removedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadius,
    },
    'del.cm-deletedLine, del, del:not(:has(.cm-deletedText))': {
        textDecoration: generalDiff.deletedTextDecoration,
        backgroundColor: `${removedBackground} !important`,
        color: removedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadius,
        border: `1px solid ${removedText}40`,
    },
    'del .cm-deletedText, del .cm-changedText': {
        background: 'transparent !important',
    },
    // Tooltips and autocomplete
    '.cm-tooltip': {
        backgroundColor: tooltipBackground,
        border: `1px solid ${base02}`,
        borderRadius: generalTooltip.borderRadius,
        padding: generalTooltip.padding,
        boxShadow: '0 2px 8px rgba(0, 0, 0, 0.3)',
    },
    '.cm-tooltip-autocomplete': {
        '& > ul': {
            backgroundColor: tooltipBackground,
            border: 'none',
        },
        '& > ul > li': {
            padding: generalTooltip.padding,
            lineHeight: generalTooltip.lineHeight,
        },
        '& > ul > li[aria-selected]': {
            backgroundColor: base01,
            color: base05,
            borderRadius: generalTooltip.borderRadiusSelected,
        },
        '& > ul > li > span.cm-completionIcon': {
            color: base02,
            paddingRight: generalTooltip.paddingRight,
        },
        '& > ul > li > span.cm-completionDetail': {
            color: base02,
            fontStyle: 'italic',
        },
    },
    '.cm-tooltip .cm-tooltip-arrow:before': {
        borderTopColor: 'transparent',
        borderBottomColor: 'transparent',
    },
    '.cm-tooltip .cm-tooltip-arrow:after': {
        borderTopColor: tooltipBackground,
        borderBottomColor: tooltipBackground,
    },
    // Diagnostics styling
    '.cm-diagnostic': {
        '&-error': {
            borderLeft: `3px solid ${red}`,
        },
        '&-warning': {
            borderLeft: `3px solid ${diagnosticWarning}`,
        },
        '&-info': {
            borderLeft: `3px solid ${linkColor}`,
        },
    },
    '.cm-lintPoint-error': {
        borderBottom: `2px wavy ${red}`,
    },
    '.cm-lintPoint-warning': {
        borderBottom: `2px wavy ${diagnosticWarning}`,
    },
    // Matching brackets
    '.cm-matchingBracket': {
        backgroundColor: activeBracketBg,
        outline: `1px solid ${activeBracketBorder}`,
        borderRadius: generalMatching.borderRadius,
    },
    '.cm-nonmatchingBracket': {
        backgroundColor: `${red}40`,
        outline: `1px solid ${invalid}`,
        borderRadius: generalMatching.borderRadius,
    },
    // Selection matches
    '.cm-selectionMatch': {
        backgroundColor: selectionMatch,
        outline: `1px solid ${base01}50`,
        borderRadius: generalMatching.borderRadius,
    },
    // Fold placeholder
    '.cm-foldPlaceholder': {
        backgroundColor: tooltipBackground,
        color: base02,
        border: `1px dotted ${base02}70`,
        borderRadius: generalPlaceholder.borderRadius,
        padding: generalPlaceholder.padding,
        margin: generalPlaceholder.margin,
    },
    // Focus outline
    '&.cm-focused': {
        outline: 'none',
        boxShadow: `0 0 0 2px ${background}, 0 0 0 3px ${lavender}40`,
    },
    // Scrollbars
    '& .cm-scroller::-webkit-scrollbar': {
        width: generalScroller.width,
        height: generalScroller.height,
    },
    '& .cm-scroller::-webkit-scrollbar-track': {
        background: darkBackground,
    },
    '& .cm-scroller::-webkit-scrollbar-thumb': {
        backgroundColor: base01,
        borderRadius: generalScroller.borderRadius,
        border: `3px solid ${darkBackground}`,
    },
    '& .cm-scroller::-webkit-scrollbar-thumb:hover': {
        backgroundColor: base02,
    },
    // Ghost text
    '.cm-ghostText': {
        opacity: '0.5',
        color: base02,
    },
}, { dark: true });
/**
 * Enhanced syntax highlighting for Catppuccin Mocha theme
 */
const catppuccinMochaHighlightStyle = language.HighlightStyle.define([
    // Keywords and control flow
    { tag: highlight.tags.keyword, color: mauve, fontWeight: 'bold' },
    { tag: highlight.tags.controlKeyword, color: mauve, fontWeight: 'bold' },
    { tag: highlight.tags.moduleKeyword, color: mauve, fontWeight: 'bold' },
    // Names and variables
    { tag: [highlight.tags.name, highlight.tags.deleted, highlight.tags.character, highlight.tags.macroName], color: base04 },
    { tag: [highlight.tags.variableName], color: base04 },
    { tag: [highlight.tags.propertyName], color: blue, fontStyle: 'normal' },
    // Classes and types
    { tag: [highlight.tags.typeName], color: yellow },
    { tag: [highlight.tags.className], color: yellow, fontStyle: 'italic' },
    { tag: [highlight.tags.namespace], color: peach, fontStyle: 'italic' },
    // Operators and punctuation
    { tag: [highlight.tags.operator, highlight.tags.operatorKeyword], color: sky },
    { tag: [highlight.tags.bracket], color: base03 },
    { tag: [highlight.tags.brace], color: teal },
    { tag: [highlight.tags.punctuation], color: base03 },
    // Functions and parameters
    { tag: [highlight.tags.function(highlight.tags.variableName), highlight.tags.labelName], color: blue },
    { tag: [highlight.tags.definition(highlight.tags.function(highlight.tags.variableName))], color: blue },
    { tag: [highlight.tags.definition(highlight.tags.variableName)], color: peach },
    // Constants and literals
    { tag: highlight.tags.number, color: peach },
    { tag: highlight.tags.changed, color: yellow },
    { tag: highlight.tags.annotation, color: red, fontStyle: 'italic' },
    { tag: highlight.tags.modifier, color: mauve, fontStyle: 'italic' },
    { tag: highlight.tags.self, color: red },
    { tag: [highlight.tags.color, highlight.tags.constant(highlight.tags.name), highlight.tags.standard(highlight.tags.name)], color: peach },
    { tag: [highlight.tags.atom, highlight.tags.bool, highlight.tags.special(highlight.tags.variableName)], color: peach },
    // Strings and regex
    { tag: [highlight.tags.processingInstruction, highlight.tags.inserted], color: green },
    { tag: [highlight.tags.special(highlight.tags.string), highlight.tags.regexp], color: pink },
    { tag: highlight.tags.string, color: green },
    // Punctuation and structure
    { tag: highlight.tags.definition(highlight.tags.typeName), color: yellow, fontWeight: 'bold' },
    { tag: [highlight.tags.definition(highlight.tags.name), highlight.tags.separator], color: flamingo },
    // Comments and documentation
    { tag: highlight.tags.meta, color: base02 },
    { tag: highlight.tags.comment, fontStyle: 'italic', color: base02 },
    { tag: highlight.tags.docComment, fontStyle: 'italic', color: base02 },
    // HTML/XML elements
    { tag: [highlight.tags.tagName], color: pink },
    { tag: [highlight.tags.attributeName], color: yellow },
    // Markdown and text formatting
    { tag: [highlight.tags.heading], fontWeight: 'bold', color: mauve },
    { tag: [highlight.tags.strong], fontWeight: 'bold', color: maroon },
    { tag: [highlight.tags.emphasis], fontStyle: 'italic', color: pink },
    // Links and URLs
    {
        tag: [highlight.tags.link],
        color: visitedLinkColor,
        fontWeight: '500',
        textDecoration: 'underline',
        textUnderlinePosition: 'under',
    },
    {
        tag: [highlight.tags.url],
        color: linkColor,
        textDecoration: 'underline',
        textUnderlineOffset: '2px',
    },
    // Special states
    {
        tag: [highlight.tags.invalid],
        color: red,
        textDecoration: 'underline wavy',
        borderBottom: `1px wavy ${red}`,
    },
    { tag: [highlight.tags.strikethrough], color: red, textDecoration: 'line-through' },
    // Enhanced syntax highlighting
    { tag: highlight.tags.constant(highlight.tags.name), color: peach },
    { tag: highlight.tags.deleted, color: red },
    { tag: highlight.tags.squareBracket, color: maroon },
    { tag: highlight.tags.angleBracket, color: sky },
    // Additional specific styles
    { tag: highlight.tags.monospace, color: teal, fontStyle: 'italic' },
    { tag: [highlight.tags.contentSeparator], color: sapphire },
    { tag: highlight.tags.quote, color: lavender },
]);
/**
 * Combined Catppuccin Mocha theme extension
 */
const catppuccinMocha = [
    catppuccinMochaTheme,
    language.syntaxHighlighting(catppuccinMochaHighlightStyle),
];
/**
 * Catppuccin Mocha merge revert styles configuration
 */
const catppuccinMochaMergeStyles = {
    backgroundColor: darkBackground,
    borderColor: base02,
    buttonColor: base04,
    buttonHoverColor: base01,
};

exports.applyMergeRevertStyles = applyMergeRevertStyles;
exports.catppuccinMocha = catppuccinMocha;
exports.catppuccinMochaMergeStyles = catppuccinMochaMergeStyles;
