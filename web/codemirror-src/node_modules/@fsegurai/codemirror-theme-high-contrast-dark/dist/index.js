import { EditorView } from '@codemirror/view';
import { HighlightStyle, syntaxHighlighting } from '@codemirror/language';
import { tags } from '@lezer/highlight';

// Helper module for styling options
const generalContent = {
    fontSize: '14px',
    fontFamily: 'JetBrains Mono, Consolas, monospace',
    lineHeight: '1.6',
};
const generalDiff = {
    insertedTextDecoration: 'none',
    deletedTextDecoration: 'line-through',
    insertedLinePadding: '1px 3px',
    borderRadius: '3px'};
const generalGutter = {
    border: 'none',
    paddingRight: '8px',
    fontSize: '0.9em',
    lineHeight: '1.78', // Adjusted to compensate for 0.9em fontSize (1.6 / 0.9 ≈ 1.78)
};
const generalPanel = {
    borderRadius: '4px',
    padding: '2px 10px',
};
const generalLine = {
    borderRadius: '2px',
};
const generalMatching = {
    borderRadius: '2px',
};
const generalPlaceholder = {
    borderRadius: '4px',
    padding: '0 5px',
    margin: '0 2px',
};
const generalScroller = {
    width: '12px',
    height: '12px',
    borderRadius: '6px',
};
const generalSearchField = {
    borderRadius: '4px',
    padding: '2px 6px',
};
const generalTooltip = {
    borderRadius: '4px',
    borderRadiusSelected: '3px',
    lineHeight: '1.3',
    padding: '4px 8px',
    paddingRight: '8px',
};
/**
 * Function to apply merge revert styles for a theme
 * @param styles Styles for the merge revert buttons
 * @param styles.backgroundColor Background color of the revert area
 * @param styles.borderColor Border color of the revert area
 * @param styles.buttonColor Color of the revert buttons
 * @param styles.buttonHoverColor Hover color of the revert buttons
 */
function applyMergeRevertStyles(styles) {
    // Create a stylesheet
    const styleEl = document.createElement('style');
    styleEl.id = 'cm-merge-revert-styles';
    // Define CSS with the theme-specific values
    styleEl.textContent = `
    .cm-merge-revert {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      padding: 4px;
      background-color: ${styles.backgroundColor};
      border-left: 1px solid ${styles.borderColor};
      border-right: 1px solid ${styles.borderColor};
      width: 32px;
    }
    
    .cm-merge-revert button {
      width: 100%;
      height: auto;
      background-color: transparent;
      border: none;
      color: ${styles.buttonColor};
      cursor: pointer;
      margin: 0 auto;
      font-size: 20px;
    }
    
    .cm-merge-revert button:hover {
      background-color: ${styles.buttonHoverColor};
    }
  `;
    // Remove any existing merge styles
    const existingStyle = document.getElementById('cm-merge-revert-styles');
    if (existingStyle)
        existingStyle.remove();
    // Add the new styles
    document.head.appendChild(styleEl);
}

/**
 * High Contrast Dark theme color definitions
 * ------------------------------------------
 * Maximum contrast theme for accessibility
 */
// Base colors - Pure black and white for maximum contrast
const base00 = '#000000', // Background - pure black
base01 = '#0a0a0a', // Darker background
base02 = '#1f1f1f', // Selection background
base03 = '#808080', // Comments - medium grey
base04 = '#b0b0b0', // Dark grey
base05 = '#ffffff', // Foreground - pure white
// High contrast accent colors
brightYellow = '#ffff00', brightCyan = '#00ffff', brightGreen = '#00ff00', brightRed = '#ff0000', brightMagenta = '#ff00ff', brightBlue = '#0099ff', brightOrange = '#ff9900', brightPurple = '#cc66ff';
// UI specific colors
const invalid = brightRed, darkBackground = base00, background = base00, tooltipBackground = '#1a1a1a', selection = '#264f78', selectionMatch = '#264f7880', cursor = brightYellow, activeBracketBg = '#1f1f1f', activeBracketBorder = brightCyan, diagnosticWarning = brightYellow, linkColor = brightCyan, visitedLinkColor = brightMagenta;
// Diff/merge specific colors
const addedBackground = '#00550060', removedBackground = '#55000060', addedText = brightGreen, removedText = brightRed;
/**
 * Enhanced editor theme styles for High Contrast Dark
 */
const highContrastDarkTheme = /*@__PURE__*/EditorView.theme({
    // Base editor styles
    '&': {
        color: base05,
        backgroundColor: background,
        fontSize: generalContent.fontSize,
        fontFamily: generalContent.fontFamily,
        border: `1px solid ${base04}`,
    },
    // Content and cursor
    '.cm-content': {
        caretColor: cursor,
        lineHeight: generalContent.lineHeight,
    },
    '.cm-cursor, .cm-dropCursor': {
        borderLeftColor: cursor,
        borderLeftWidth: '3px',
    },
    '.cm-fat-cursor': {
        backgroundColor: cursor,
        color: background,
        border: `2px solid ${base05}`,
    },
    // Selection
    '&.cm-focused > .cm-scroller > .cm-selectionLayer .cm-selectionBackground, .cm-selectionBackground, .cm-content ::selection': {
        backgroundColor: '#3a5a8a',
        outline: `2px solid ${brightCyan}`,
    },
    // Make sure selection appears above active line
    '.cm-selectionLayer': {
        zIndex: 100,
    },
    // Search functionality
    '.cm-searchMatch': {
        backgroundColor: '#006400',
        outline: `2px solid ${brightGreen}`,
        color: base05,
        borderRadius: generalSearchField.borderRadius,
        '& span': {
            color: base05,
        },
    },
    '.cm-searchMatch.cm-searchMatch-selected': {
        backgroundColor: brightGreen,
        color: base00,
        padding: generalSearchField.padding,
        outline: `2px solid ${base05}`,
        '& span': {
            color: base00,
        },
    },
    '.cm-search.cm-panel.cm-textfield': {
        color: base05,
        backgroundColor: base01,
        border: `1px solid ${base04}`,
        borderRadius: generalSearchField.borderRadius,
        padding: generalSearchField.padding,
    },
    // Panels
    '.cm-panels': {
        backgroundColor: darkBackground,
        color: base05,
        borderRadius: '4px',
        border: `1px solid ${base04}`,
    },
    '.cm-panels.cm-panels-top': {
        borderBottom: `2px solid ${base04}`,
    },
    '.cm-panels.cm-panels-bottom': {
        borderTop: `2px solid ${base04}`,
    },
    '.cm-panel button': {
        backgroundColor: tooltipBackground,
        color: brightCyan,
        border: `1px solid ${base04}`,
        borderRadius: generalPanel.borderRadius,
        padding: generalPanel.padding,
    },
    '.cm-panel button:hover': {
        backgroundColor: base02,
        border: `1px solid ${brightCyan}`,
    },
    // Line highlighting
    '.cm-activeLine': {
        backgroundColor: '#2a2a2aa6',
        borderRadius: generalLine.borderRadius,
        borderLeft: `3px solid ${brightCyan}`,
        borderRight: `1px solid ${base04}`,
        zIndex: 1,
    },
    // Gutters
    '.cm-gutters': {
        backgroundColor: darkBackground,
        color: base04,
        border: generalGutter.border,
        borderRight: `2px solid ${base04}`,
        paddingRight: generalGutter.paddingRight,
    },
    '.cm-activeLineGutter': {
        backgroundColor: base02,
        color: brightYellow,
        fontWeight: 'bold',
        border: `1px solid ${base03}`,
    },
    '.cm-lineNumbers': {
        fontSize: generalGutter.fontSize,
        lineHeight: generalGutter.lineHeight,
    },
    '.cm-foldGutter': {
        fontSize: generalGutter.fontSize,
        lineHeight: generalGutter.lineHeight,
    },
    '.cm-foldGutter .cm-gutterElement': {
        color: base04,
        cursor: 'pointer',
    },
    '.cm-foldGutter .cm-gutterElement:hover': {
        color: brightCyan,
    },
    // Diff/Merge View Styles
    '.cm-insertedLine': {
        textDecoration: generalDiff.insertedTextDecoration,
        backgroundColor: addedBackground,
        color: addedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadius,
        border: `1px solid ${addedText}`,
    },
    'ins.cm-insertedLine, ins.cm-insertedLine:not(:has(.cm-changedText))': {
        textDecoration: generalDiff.insertedTextDecoration,
        backgroundColor: `${addedBackground} !important`,
        color: addedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadius,
        border: `1px solid ${addedText}`,
    },
    'ins.cm-insertedLine .cm-changedText': {
        background: 'transparent !important',
    },
    '.cm-deletedLine': {
        textDecoration: generalDiff.deletedTextDecoration,
        backgroundColor: removedBackground,
        color: removedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadius,
        border: `1px solid ${removedText}`,
    },
    'del.cm-deletedLine, del, del:not(:has(.cm-deletedText))': {
        textDecoration: generalDiff.deletedTextDecoration,
        backgroundColor: `${removedBackground} !important`,
        color: removedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadius,
        border: `1px solid ${removedText}`,
    },
    'del .cm-deletedText, del .cm-changedText': {
        background: 'transparent !important',
    },
    // Tooltips and autocomplete
    '.cm-tooltip': {
        backgroundColor: tooltipBackground,
        border: `2px solid ${brightCyan}`,
        borderRadius: generalTooltip.borderRadius,
        padding: generalTooltip.padding,
        color: base05,
    },
    '.cm-tooltip-autocomplete': {
        '& > ul': {
            backgroundColor: tooltipBackground,
            border: `1px solid ${base04}`,
        },
        '& > ul > li': {
            padding: generalTooltip.padding,
            lineHeight: generalTooltip.lineHeight,
        },
        '& > ul > li[aria-selected]': {
            backgroundColor: selection,
            color: brightYellow,
            borderRadius: generalTooltip.borderRadiusSelected,
            border: `1px solid ${brightCyan}`,
        },
        '& > ul > li > span.cm-completionIcon': {
            color: brightCyan,
            paddingRight: generalTooltip.paddingRight,
        },
        '& > ul > li > span.cm-completionDetail': {
            color: brightYellow,
            fontStyle: 'italic',
        },
    },
    '.cm-tooltip .cm-tooltip-arrow:before': {
        borderTopColor: 'transparent',
        borderBottomColor: 'transparent',
    },
    '.cm-tooltip .cm-tooltip-arrow:after': {
        borderTopColor: brightCyan,
        borderBottomColor: brightCyan,
    },
    // Diagnostics styling
    '.cm-diagnostic': {
        '&-error': {
            borderLeft: `4px solid ${brightRed}`,
            backgroundColor: '#33000020',
        },
        '&-warning': {
            borderLeft: `4px solid ${diagnosticWarning}`,
            backgroundColor: '#33330020',
        },
        '&-info': {
            borderLeft: `4px solid ${linkColor}`,
            backgroundColor: '#00333320',
        },
    },
    '.cm-lintPoint-error': {
        borderBottom: `3px wavy ${brightRed}`,
    },
    '.cm-lintPoint-warning': {
        borderBottom: `3px wavy ${diagnosticWarning}`,
    },
    // Matching brackets
    '.cm-matchingBracket': {
        backgroundColor: activeBracketBg,
        outline: `2px solid ${activeBracketBorder}`,
        borderRadius: generalMatching.borderRadius,
    },
    '.cm-nonmatchingBracket': {
        backgroundColor: '#33000050',
        outline: `2px solid ${invalid}`,
        borderRadius: generalMatching.borderRadius,
    },
    // Selection matches
    '.cm-selectionMatch': {
        backgroundColor: selectionMatch,
        outline: `2px solid ${brightCyan}50`,
        borderRadius: generalMatching.borderRadius,
    },
    // Fold placeholder
    '.cm-foldPlaceholder': {
        backgroundColor: tooltipBackground,
        color: brightCyan,
        border: `2px solid ${brightCyan}`,
        borderRadius: generalPlaceholder.borderRadius,
        padding: generalPlaceholder.padding,
        margin: generalPlaceholder.margin,
    },
    // Focus outline
    '&.cm-focused': {
        outline: `3px solid ${brightCyan}`,
        outlineOffset: '2px',
    },
    // Scrollbars
    '& .cm-scroller::-webkit-scrollbar': {
        width: generalScroller.width,
        height: generalScroller.height,
    },
    '& .cm-scroller::-webkit-scrollbar-track': {
        background: darkBackground,
        border: `1px solid ${base04}`,
    },
    '& .cm-scroller::-webkit-scrollbar-thumb': {
        backgroundColor: base04,
        border: `2px solid ${base05}`,
        borderRadius: generalScroller.borderRadius,
    },
    '& .cm-scroller::-webkit-scrollbar-thumb:hover': {
        backgroundColor: brightCyan,
    },
    // Ghost text
    '.cm-ghostText': {
        opacity: '0.7',
        color: base03,
    },
}, { dark: true });
/**
 * Enhanced syntax highlighting for High Contrast Dark theme
 */
const highContrastDarkHighlightStyle = /*@__PURE__*/HighlightStyle.define([
    // Keywords and control flow
    { tag: tags.keyword, color: brightMagenta, fontWeight: 'bold' },
    { tag: tags.controlKeyword, color: brightMagenta, fontWeight: 'bold' },
    { tag: tags.moduleKeyword, color: brightMagenta, fontWeight: 'bold' },
    // Names and variables
    { tag: [tags.name, tags.deleted, tags.character, tags.macroName], color: base05 },
    { tag: [tags.variableName], color: base05 },
    { tag: [tags.propertyName], color: brightCyan, fontStyle: 'normal' },
    // Classes and types
    { tag: [tags.typeName], color: brightYellow },
    { tag: [tags.className], color: brightYellow, fontStyle: 'italic' },
    { tag: [tags.namespace], color: brightOrange, fontStyle: 'italic' },
    // Operators and punctuation
    { tag: [tags.operator, tags.operatorKeyword], color: brightCyan },
    { tag: [tags.bracket], color: brightYellow },
    { tag: [tags.brace], color: brightCyan },
    { tag: [tags.punctuation], color: base05 },
    // Functions and parameters
    { tag: [/*@__PURE__*/tags.function(tags.variableName), tags.labelName], color: brightBlue },
    { tag: [/*@__PURE__*/tags.definition(/*@__PURE__*/tags.function(tags.variableName))], color: brightBlue },
    { tag: [/*@__PURE__*/tags.definition(tags.variableName)], color: brightOrange },
    // Constants and literals
    { tag: tags.number, color: brightOrange },
    { tag: tags.changed, color: brightYellow },
    { tag: tags.annotation, color: brightRed, fontStyle: 'italic' },
    { tag: tags.modifier, color: brightPurple, fontStyle: 'italic' },
    { tag: tags.self, color: brightRed },
    {
        tag: [tags.color, /*@__PURE__*/tags.constant(tags.name), /*@__PURE__*/tags.standard(tags.name)],
        color: brightOrange,
    },
    { tag: [tags.atom, tags.bool, /*@__PURE__*/tags.special(tags.variableName)], color: brightOrange },
    // Strings and regex
    { tag: [tags.processingInstruction, tags.inserted], color: brightGreen },
    { tag: [/*@__PURE__*/tags.special(tags.string), tags.regexp], color: brightMagenta },
    { tag: tags.string, color: brightGreen },
    // Punctuation and structure
    { tag: /*@__PURE__*/tags.definition(tags.typeName), color: brightYellow, fontWeight: 'bold' },
    { tag: [/*@__PURE__*/tags.definition(tags.name), tags.separator], color: brightCyan },
    // Comments and documentation
    { tag: tags.meta, color: base03 },
    { tag: tags.comment, fontStyle: 'italic', color: base03 },
    { tag: tags.docComment, fontStyle: 'italic', color: base03 },
    // HTML/XML elements
    { tag: [tags.tagName], color: brightMagenta },
    { tag: [tags.attributeName], color: brightYellow },
    // Markdown and text formatting
    { tag: [tags.heading], fontWeight: 'bold', color: brightMagenta },
    { tag: [tags.strong], fontWeight: 'bold', color: brightOrange },
    { tag: [tags.emphasis], fontStyle: 'italic', color: brightMagenta },
    // Links and URLs
    {
        tag: [tags.link],
        color: visitedLinkColor,
        fontWeight: '500',
        textDecoration: 'underline',
        textUnderlinePosition: 'under',
    },
    {
        tag: [tags.url],
        color: linkColor,
        textDecoration: 'underline',
        textUnderlineOffset: '2px',
    },
    // Special states
    {
        tag: [tags.invalid],
        color: brightRed,
        textDecoration: 'underline wavy',
        borderBottom: `2px wavy ${brightRed}`,
    },
    { tag: [tags.strikethrough], color: brightRed, textDecoration: 'line-through' },
    // Enhanced syntax highlighting
    { tag: /*@__PURE__*/tags.constant(tags.name), color: brightOrange },
    { tag: tags.deleted, color: brightRed },
    { tag: tags.squareBracket, color: brightYellow },
    { tag: tags.angleBracket, color: brightCyan },
    // Additional specific styles
    { tag: tags.monospace, color: brightBlue, fontStyle: 'italic' },
    { tag: [tags.contentSeparator], color: brightPurple },
    { tag: tags.quote, color: brightPurple },
]);
/**
 * Combined High Contrast Dark theme extension
 */
const highContrastDark = [
    highContrastDarkTheme,
    /*@__PURE__*/syntaxHighlighting(highContrastDarkHighlightStyle),
];
/**
 * High Contrast Dark merge revert styles configuration
 */
const highContrastDarkMergeStyles = {
    backgroundColor: darkBackground,
    borderColor: brightCyan,
    buttonColor: brightCyan,
    buttonHoverColor: brightYellow,
};

export { applyMergeRevertStyles, highContrastDark, highContrastDarkMergeStyles };
